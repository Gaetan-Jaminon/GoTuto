# Default configuration for billing database migrations

database:
  host: localhost
  port: 5432
  user: postgres
  password: password
  name: billing
  sslmode: disable

migrations:
  path: ./migrations