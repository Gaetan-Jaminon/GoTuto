apiVersion: v1
kind: Secret
metadata:
  name: demo01-secrets
  namespace: default
type: Opaque
stringData:
  # Database credentials
  # In production, use external secret management (Vault, AWS Secrets Manager, etc.)
  DEMO01_DATABASE_PASSWORD: "your-secure-password-here"
  
  # Other sensitive configuration
  # DEMO01_JWT_SECRET: "your-jwt-secret-here"
  # DEMO01_API_KEY: "your-api-key-here"