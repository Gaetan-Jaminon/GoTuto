server:
  mode: "debug"
  port: 8080

database:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "postgres"
  ssl_mode: "disable"

logging:
  level: "debug"
  format: "text"

cors:
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
    - "http://127.0.0.1:3000"